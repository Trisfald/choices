[package]
name = "util"
version = "0.1.0"
authors = ["trisfald"]
edition = "2018"

[dependencies]
reqwest = "0.11"
rand = "0.8"
tokio = { version = "1.0", features = ["macros", "rt-multi-thread"] }
choices = { path = "../", features = ["json"] }
